---
artifact: ${project.groupId}:${project.artifactId}:${project.version}
name: "SmallRye Reactive Messaging PubSub"
metadata:
  keywords:
    - "pubsub"
    - "google cloud"
    - "google pubsub"
    - "google"
  categories:
    - "messaging"
  guide: "https://quarkus.io/guides/"
  status: "experimental"
  config:
    - "quarkus.google.cloud.pubsub."
    - "mp.messaging."
